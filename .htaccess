Options +FollowSymLinks
RewriteEngine On

# Mantener la estructura REAL de las carpetas
RewriteBase /

# ✅ Permitir PHP normales
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# ✅ Permitir acceso a carpetas
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# ✅ Permitir archivos estáticos
RewriteCond %{REQUEST_URI} \.(css|js|png|jpg|jpeg|gif|svg|ico|webp|woff|woff2|ttf|eot|pdf|txt)$ [NC]
RewriteRule ^ - [L]

# ❌ QUITAMOS EL REDIRECT A index.php (esto es lo que rompía todo)
